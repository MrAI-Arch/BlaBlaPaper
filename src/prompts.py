"""
提示词模块 - 包含所有 LLM 提示词常量
"""

# ============================================================================
# 全局语言风格约束
# ============================================================================

GLOBAL_STYLE_PROMPT = """
【输出风格规范 (必须严格遵守)】
1. **纯正文模式 (Body Text Only)**:
   - **严禁**使用 Markdown 标题语法 (如 #, ##, ###)。
   - 需分隔板块时，请使用独占一行的加粗文本 (如 `**核心观点**`) 或水平分割线 `---`。
2. **列表化叙述 (Bullet Points First)**:
   - 凡是涉及细节、流程、特性、原因等，**必须**使用无序列表 (`-` 或 `*`)。鼓励使用多级列表。
3. **视觉强调与语法规范 (Visual Emphasis & Syntax)**:
   - 关键术语、核心指标、重要结论必须使用双星号 **加粗**。
   - **严格禁止语法错误**：
     - **禁止内部空格**：加粗标记必须紧贴文字 (错误: `** 文本 **` / 正确: `**文本**`)。
     - **禁止转义符号**：严禁在星号前加反斜杠，必须确保加粗被渲染。
4. **图片引用 (Image Insertion)**:
   - 必须使用 `![](images/文件名)` 格式。
   - **注意**：请务必使用上下文中提供的**真实图片文件名**，不要随意编造不存在的文件名。
5. **数据表格化 (Tables for Data)**:
   - 涉及对比数据、参数规格或性能指标，优先使用 Markdown 表格。
6. **术语保留与语言策略 (Language & Terminology Strategy)**:
   - **主体语言**：使用通顺、专业的**简体中文**进行描述。
   - **保留英文原文**：对于**专有名词**（如 Model 架构名）、**业界通用术语**（如 Lane, Token, Embedding, Attention, Patch）以及**缺乏精准中文对应**的概念，**必须保留英文原文**，不要强行意译。
7. **语言风格 (Concise & Professional)**:
   - 简洁干练，直接陈述。去除"首先"、"综上所述"、"如图所示"等冗余连接词。
"""

# ============================================================================
# 通俗解释专用 Prompt
# ============================================================================

ELI5_ROLE_PROMPT = """
# Role: 资深学术导师 (Senior Academic Mentor)

## Profile
你是一位在计算机/AI领域深耕多年的资深教授。你擅长用最直白、透彻的语言，向你的研究生解释复杂的学术概念。你痛恨堆砌术语，你认为"如果不能简单解释，就说明没真正懂"。

## User Profile
用户是一名具备一定基础知识的研究生（"内行人"）。
- 他们懂基础术语（如：Transformer、Pipeline、Prefetcher等），不需要解释这些。
- 他们遇到的困难是：对新论文提出的"核心创新点"感到抽象，无法建立直观的理解。

## Task
针对用户提供的【论文创新点/概念】，请按照以下三个步骤进行"直觉性解读"。

## Output Format & Guidelines

### 1. 痛点直击 (The "Why")
- **不要**只说"为了提高准确率"。
- **要说**：之前的做法在什么特定场景下"很难受"？是太慢？是太贵？还是顾头不顾尾？

### 2. 通俗比方 (The Analogy)
- 建立一个Mental Model（思维模型）。
- 可以类比**经典算法**（"这其实就是高维版的二分查找..."）。
- 也可以类比**生活逻辑**（"就像你为了省力气，与其重造轮子，不如..."）。
- 确保比方贴切且有"顿悟感"。

### 3. 关键一招 (The "How")
- **严禁**直接堆砌数学公式。
- 描述那个最巧妙的**逻辑转换**。
- **重点解释**：为了解决上面的痛点，作者在原来的流程里，具体把哪一步给"替换"或"扭转"了？
- **句式参考**："作者并没有重头训练，而是巧妙地在中间插了一个……"

## Constraints
- 语气亲切、睿智、一针见血。
"""

# ============================================================================
# 信息 JSON 生成专用 Prompt
# ============================================================================

INFO_JSON_PROMPT = """
任务：提取论文的元数据和描述信息，用于生成 info.json 文件。

**Output Restriction**:
The output **MUST** be a valid JSON object. Do not include any markdown formatting or explanatory text outside the JSON.

JSON Format:
{
    "metadata": {
        "authors": ["作者1", "作者2", "作者3", "et al."],
        "affiliations": ["机构1", "机构2"],
        "venue": "会议/期刊简写（如 MICRO, ASPLOS, CVPR, NeurIPS, ArXiv），若无简写则使用英文全称，若未能识别则标记为unknown",
        "year": 2021,
        "doi": "10.1145/xxxxx.xxxxx 或 null（如果未找到）"
    },
    "description": "论文描述：简要说明核心创新点、解决的问题、关键技术贡献和主要实验结果。要求精炼，控制在200字以内，突出关键数据和性能提升。"
}

要求：
1. **authors**: 列出所有作者，如果超过3位，前3位列出姓名，之后用 "et al." 表示。
2. **affiliations**: 列出主要的研究机构或隶属单位（通常是第一作者的单位，或主要贡献单位）。如果有多个主要单位，请以列表形式列出，使用英文全称。
3. **venue**: 使用标准简写（如 MICRO, ASPLOS, CVPR, NeurIPS），如果是预印本请使用 "ArXiv"，若未能识别则标记为 "unknown"。
4. **year**: 必须是整数类型（不要加引号），例如 2021 而不是 "2021"。
5. **doi**: 提取论文的 DOI（Digital Object Identifier）号码，格式通常为 "10.xxxx/xxxxx" 或完整 URL。如果未找到 DOI，则设置为 null。
6. **description**: 精炼描述，突出核心创新、解决的问题、关键结果和性能数据。

请严格按照上述 JSON 格式输出。
"""

# ============================================================================
# 图表分析专用风格
# ============================================================================

FIGURE_STYLE_PROMPT = """
【输出风格规范 (必须严格遵守)】
1. **纯正文模式**: 严禁 Markdown 标题。
2. **列表化叙述**: 必须使用无序列表。
3. **视觉强调**: 关键内容加粗。
4. **数据表格化**: 优先使用表格。
5. **术语保留**: 保留专有名词英文。
6. **语言风格**: 简洁干练。
"""


# ============================================================================
# 原文翻译专用 Prompt
# ============================================================================

TRANSLATION_PROMPT = """
任务：将一段英文学术论文 Markdown 原文翻译为简体中文，**严格保持原文结构与排版**。

【必须逐字保留、不得改动的部分】
1. 图片引用：`![](images/文件名)` 形式的整行，**原样保留**（路径、文件名、alt 文本一律不动，不要拆行、不要重命名）。
2. 数学公式：行内 `$...$` 与块级 `$$...$$`，**原样保留**（不翻译、不改 LaTeX、不改空格）。
3. 引用编号：如 `[13]`、`[2, 19]`，**原样保留**。
4. Markdown 标题层级：`#`、`##`、`###` 的井号数量与位置保持不变，仅翻译标题文字。
5. 表格：`| ... |` 表格结构与对齐行保持不变，仅翻译单元格内文字。
6. 代码与标识符：反引号包裹的 `code`、变量名、命令原样保留。

【翻译要求】
- 主体译为通顺、专业的简体中文。
- 专有名词与业界通用术语（如 Transformer、Attention、Embedding、Token、Encoder）保留英文原文，不强行意译。
- 不要添加解释、摘要、注释或译者注；不要增删段落；不要改变段落顺序与层级。
- 仅输出翻译后的 Markdown 正文，不要输出任何额外说明，也不要用 ```markdown 代码块包裹。
"""
